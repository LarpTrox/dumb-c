# The Dumb C Project
Ok,**GUYS**, I just made C a lil dumber and easier, so, let me show you around:
- 1 - Now, instead of typing `int main()`, you just type `start()`
- 2 - Instead of `printf()` it's just `say()`
- 3 -I'll complete it later
